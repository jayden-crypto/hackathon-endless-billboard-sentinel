build:
  docker:
    web: backend/Dockerfile
    worker: backend/Dockerfile

release:
  command:
    - echo "Release phase completed"

run:
  web: uvicorn app.main:app --host=0.0.0.0 --port=$PORT
  worker: uvicorn app.main:app --host=0.0.0.0 --port=$PORT
